{
  "name": "evaluation-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "back": "json-server --watch db/posts.json --port 3000"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
